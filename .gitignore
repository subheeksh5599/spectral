# dependencies / build
node_modules/
.next/
out/
cache/
broadcast/
# lib/forge-std is deliberately NOT ignored: it is vendored and committed, so `git clone`
# followed by `forge test` works with no install step and no submodule flag.

# secrets — never committed
.env
.env.*
!.env.example

# local
.DS_Store
*.log
dist/
app/node_modules/

# npm pack artefacts are built, not committed
*.tgz
